app: ts-eks
provider: aws
path: .
out_dir: compiled
defaults:
    execution_unit:
        type: eks
        pulumi_params:
            nodeType: node
            replicas: 1
            nodeConstraints:
                instanceType: c4.large
                diskSize: 200
